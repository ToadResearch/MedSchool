[project]
name = "medschool-mcp-demo"
version = "0.1.0"
description = "A simple tool demo using MCP"
readme = "README.md"
requires-python = ">=3.12"

dependencies = [
    "openai",
    "mcp",
    "python-dotenv",
    "fastmcp",
    "pydantic"
]